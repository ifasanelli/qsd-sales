<h2 class="title"><%= @customer.name %></h2>

<table class="ls-table">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Endereço</th>
      <th>CPF</th>
      <th>Email</th>
      <th>Telefone</th>
      <th>Data de Nascimento</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= @customer.name %></td>
      <td><%= @customer.address %></td>
      <td><%= @customer.document %></td>
      <td><%= @customer.email %></td>
      <td><%= @customer.phone %></td>
      <td><%= @customer.birth_date %></td>
    </tr>
  </tbody>
</table>

<%= link_to 'Editar', edit_customer_path(@customer), class: "ls-btn", style: "color: rgb(79, 89, 216);" %>
<%= link_to 'Apagar', customer_path(@customer), method: :delete, class: "ls-btn", style: "color: rgb(105, 30, 0);",
data: { confirm: "Tem certeza de que deseja deletar #{@customer.name}?"} %>
<%= link_to 'Voltar', customers_path, class:"ls-btn" %>